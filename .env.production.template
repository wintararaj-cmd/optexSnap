# Production Environment Variables Template
# Copy these to Coolify's Environment Variables section

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Use the service name from Coolify (usually the database name you created)
DB_HOST=ruchi-db
DB_PORT=5432
DB_NAME=restaurant_db
DB_USER=postgres
DB_PASSWORD=<GET_FROM_COOLIFY_DATABASE_SETTINGS>

# ============================================
# APPLICATION CONFIGURATION
# ============================================
NEXT_PUBLIC_APP_URL=https://yourdomain.com
NODE_ENV=production
PORT=3000

# ============================================
# SECURITY
# ============================================
# Generate a strong random string (64+ characters)
# You can use: openssl rand -base64 64
JWT_SECRET=<GENERATE_STRONG_RANDOM_STRING>

# ============================================
# GOOGLE OAUTH (Optional)
# ============================================
# Get from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================
# EMAIL CONFIGURATION (Stalwart)
# ============================================
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@yourdomain.com
SMTP_PASSWORD=<EMAIL_PASSWORD_FROM_STALWART>
SMTP_FROM=noreply@yourdomain.com
SMTP_FROM_NAME=Ruchi Restaurant

# ============================================
# ADMIN CREDENTIALS
# ============================================
# These are used by the create-admin script
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=<CHOOSE_STRONG_PASSWORD>
ADMIN_NAME=Administrator

# ============================================
# OPTIONAL: MONITORING & ANALYTICS
# ============================================
# Add if you use these services
# SENTRY_DSN=
# GOOGLE_ANALYTICS_ID=
# MIXPANEL_TOKEN=

# ============================================
# NOTES
# ============================================
# 1. Replace all <PLACEHOLDERS> with actual values
# 2. Never commit this file with real values
# 3. Keep credentials secure
# 4. Use strong passwords (20+ characters)
# 5. For JWT_SECRET, use: openssl rand -base64 64
